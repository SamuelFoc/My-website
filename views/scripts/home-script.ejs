<script>

const box = document.getElementById("mainPage");

const halfWidth = window.innerWidth/2;
const halfHeight = window.innerHeight/2;

//Imitation of light 
box.addEventListener("mousemove", (e) => {
    let offsetY = halfHeight - e.clientY;
    let offsetX = halfWidth - e.clientX;
    let distance = Math.sqrt(Math.pow(offsetX, 2) + Math.pow(offsetY, 2));

    box.style.textShadow = offsetX/30 + "px " + offsetY/30 + "px " + distance/30 + "px rgba(0, 0, 0, .2)";
});

//Downscroll to horizontal scroll section
box.addEventListener("wheel", event => {
    event.preventDefault();
    if (event.deltaY > 0){
        container.scrollIntoView({behavior: "smooth"});
    }
    box.removeEventListener("wheel");
});

</script>