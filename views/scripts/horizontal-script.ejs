<script>
const screenHeight = window.innerHeight;
const container = document.getElementById("horizontal");
const firstPage = document.getElementById("firstHorizontalPage");
const secondPage = document.getElementById("secondHorizontalPage");



container.addEventListener("wheel", event => {
    event.preventDefault();

    if (event.deltaY < 0 && container.scrollLeft === 0){
        box.scrollIntoView({behavior: "smooth"});
    
    } else if (event.deltaY > 0){
        let newPositionX = container.scrollLeft + window.innerWidth;
        container.scrollTo({
            left: newPositionX, 
            top: screenHeight,
            behavior: "smooth"
        });
    
    } else if (event.deltaY < 0 && container.scrollLeft !== 0){
        let newPositionX = container.scrollLeft - window.innerWidth; 
        container.scrollTo({
            left: newPositionX, 
            top: screenHeight,
            behavior: "smooth"
        });
    }

});

</script>