<script>

const container = document.getElementById("horizontal");
const aboutMe = document.getElementById("aboutMe");
const aboutMe2 = document.getElementById("aboutMe2");
const myWork = document.getElementById("myWork");
const contacts = document.getElementById("contacts");


elementScrollToPages(aboutMe, aboutMe2, box);
elementScrollToPages(aboutMe2, myWork, aboutMe);
elementScrollToPages(myWork, contacts, aboutMe2);
elementScrollToPages(contacts, undefined, myWork);


function elementScrollToPages(element, forDownScroll, forUpScroll){
    element.addEventListener("wheel", (e) => {
        event.preventDefault();
        if (event.deltaY < 0){ 
            if (forUpScroll !== undefined) forUpScroll.scrollIntoView({behavior: "smooth"})  
        } else if (event.deltaY > 0){ 
            if (forDownScroll !== undefined) forDownScroll.scrollIntoView({behavior: "smooth"})
        };
    });
};

</script>


